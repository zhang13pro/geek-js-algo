// function depthFirstSearch(graph, start) {
//   const visited = new Set()
//   const stack = [start]
//   while (stack.length) {
//     const node = stack.pop()
//     if (!visited.has(node)) {
//       visited.add(node)
//       stack.push(...graph[node])
//     }
//   }
//   return visited
// }

function depthFirstSearch(root: Node | null) {}
